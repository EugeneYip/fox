// @ts-check
/**
 * 文案慣例的規則本體 —— 抽出來是為了**能在不跑整支腳本的情況下測**。
 *
 * `check-copy.mjs` 是一支會走檔案系統、讀 dist、import ui.ts 的腳本，
 * 一 import 它就整個跑起來。而第 6 輪（第二十一圈）加的 `subject`
 * 需要一格「拿規則自己的違規樣本驗 subject 比 bad 寬」的測試 ——
 * 那是純函式的事，不該為了它去建一份假站。
 *
 * `scripts/lib/privacy-rules.mjs` 是同一個理由抽出來的。
 */

/**
 * `subject` 是「這一行有沒有東西給這條規則判斷」。
 *
 * ── 為什麼需要它 ──────────────────────────────────
 *
 * 第二十一圈問「這一條檢查實際上判斷過多少東西」。這一支跟前幾支不一樣：
 * **每一條規則都掃過每一行**，所以「掃了幾行」對五條規則是同一個數字，
 * 那個數字什麼都沒說。
 *
 * 真正的答案是「這一行上，這條規則有沒有可能響」——
 * 一條要求兩側都是漢字的規則，在一行純英文上是**沒有東西可判斷**的。
 * 每條規則自己宣告前提，數字才有意義。
 *
 * 前提刻意寫得比 `bad` 寬（`bad` 是「違規的樣子」，`subject` 是
 * 「有機會違規的樣子」）—— 例如半形標點那條的前提只要求
 * 「有漢字、也有半形標點」，不要求它們相鄰。
 *
 * `samples` 是「一定要被抓到」的最小違規例子。
 * 它讓 `subject` 有東西可驗：`bad` 配得到的，`subject` 一定也要配得到 ——
 * 反過來（subject 寫窄了）那條規則就會對它本來抓得到的東西閉嘴，
 * 而報告會說「沒有發現問題」，看起來跟真的沒問題一模一樣。
 *
 * @type {{ id: string, bad: RegExp, why: string, subject: RegExp, samples: string[] }[]}
 */
export const RULES = [
  {
    id: 'taiwan-tai',
    bad: /台(?:灣|北|中|南|東)|平台/g,
    subject: /台/,
    samples: ['去台北玩', '這個平台很好用', '台灣的天氣'],
    why: '專案約定用「臺」不用「台」：臺灣、臺北、平臺。見 docs/CONTENT.md 的「用字的幾個約定」。',
  },
  /*
   * 底下三條在加進來的時候，全站與文件都是 **0 處** ——
   * 它們是預防性的，不是為了修既有的問題。
   *
   * 三條都刻意要求「兩側都是漢字」：
   *   `他說 "hello" 嗎` 不會被抓（引號兩側是拉丁字母），
   *   `中"文` 才會 —— 那個明顯是錯的。
   * 這樣才不會誤傷中英混排的正常寫法。
   */
  {
    id: 'halfwidth-punct',
    bad: /[一-鿿]\s*[,.;:!?]\s*[一-鿿]/g,
    subject: /[一-鿿][\s\S]*[,.;:!?]|[,.;:!?][\s\S]*[一-鿿]/,
    samples: ['今天很好,明天也是', '他說:好', '真的嗎?我不知道'],
    why: '中文句子裡夾了半形標點。標點要用全形（，。；：！？）。見 docs/CONTENT.md 的「用字的幾個約定」。',
  },
  {
    id: 'straight-quotes',
    bad: /[一-鿿]["']["']?[一-鿿]/g,
    subject: /[一-鿿][\s\S]*["']|["'][\s\S]*[一-鿿]/,
    samples: ['他說"好"啊', "她講'不'字"],
    why: '中文裡用了直引號。這個站一律用「」與『』。',
  },
  /*
   * 中文與英文**字母**之間要空一格。約定寫在兩個地方：CLAUDE.md 的「語氣」
   * （給在這裡寫程式的人）與 docs/CONTENT.md 的「用字的幾個約定」（給寫文案的人）。
   * `rule-not-documented` 要求兩份都要有，所以清單不會分岔。
   *
   * **只看字母，不看數字** —— 這是這條規則能不能用的關鍵。
   * 中文的日期格式是「9月2日」，中間本來就不空格；
   * 把數字算進來的話全站會有 295 處誤報（第 6 輪〔第五圈〕實測的數字）。
   *
   * 加這條之前量過：全站可見文字與文件都是 **0 處**，
   * 所以它是把既有的習慣釘住，不是新規定。
   */
  {
    id: 'cjk-latin-space',
    bad: /[一-鿿][A-Za-z]|[A-Za-z][一-鿿]/g,
    subject: /[一-鿿][\s\S]*[A-Za-z]|[A-Za-z][\s\S]*[一-鿿]/,
    samples: ['用Astro建的站', '這是Markdown檔', 'Astro很快'],
    why: '中文與英文字母之間要空一格：「用 Astro 建的站」。數字不適用（日期寫「9月2日」）。見 docs/CONTENT.md 的「用字的幾個約定」。',
  },
  {
    id: 'halfwidth-ellipsis',
    bad: /[一-鿿]\s*\.{3}/g,
    subject: /[一-鿿][\s\S]*\.{3}/,
    samples: ['想了很久...', '然後就 ...'],
    why: '中文裡的刪節號用 ⋯⋯（或 ……），不用三個半形句點。',
  },
  /*
   * ── 用臺灣的詞，不要用大陸的詞 ──────────────────────
   *
   * 站主 2026-09-11 定的：這個站的中文要是臺灣自然會寫的中文。
   * 這不是政治潔癖，是這個站在說誰的話 —— 一個臺灣讀中文系的人寫的站，
   * 寫出「上傳視頻」就不是她的聲音了。
   *
   * 加這條的時候實測過語料：`dist` 的可見文字 63 頁、`docs` 與三份
   * 根目錄文件，**一個命中都沒有**。所以它不是為了修現有的東西加的，
   * 是為了讓今天這個狀態留得住。
   *
   * ## 這張清單刻意很短
   *
   * 只放「臺灣不會這樣寫」而且**不會冤枉好人**的。以下這些查過之後
   * 拿掉了，理由都是同一個 —— 它們在臺灣的中文裡有正當用法：
   *
   *   質量  物理的 mass 就叫質量
   *   用戶  用戶名稱、終端用戶，臺灣照樣這樣說
   *   信息  文言裡的「音信消息」，一篇白話翻譯很可能用到
   *   水平  水平方向
   *   支持／項目／登錄／對象／循環  都是臺灣的日常用字
   *   刷新  「刷新紀錄」是臺灣的說法
   *   代碼  國家代碼、郵遞區號那種 code
   *   菜單  餐廳的菜單
   *   牀    〈靜夜思〉的「牀前明月光」就是這個字
   *   智能  智能障礙
   *
   * 引用的原文與外站標題本來就不進這支的語料（見 check-copy.mjs 的
   * QUOTED）—— 別人寫的字就是別人寫的字，這條規則管的是站上的文案。
   */
  {
    id: 'mainland-wording',
    bad: /視頻|音頻|軟件|硬件|網絡|默認|缺省|屏幕|服務器|鼠標|博客|分辨率|加載|緩存|高清|激活|內存|數碼|打印|賬|裏|爲|着|麽|衆|羣|綫/g,
    subject: /[一-鿿]/,
    samples: ['上傳視頻', '網絡不穩', '默認值', '他在那裏', '爲了你', '看着我'],
    why:
      '網站上的中文要用臺灣的詞與字：影片不是視頻、網路不是網絡、預設不是默認、' +
      '螢幕不是屏幕；裡不是裏、為不是爲、著不是着。見 docs/CONTENT.md 的「用字的幾個約定」。',
  },
];

/**
 * 哪幾條規則必須寫進那兩份文件，哪幾條不用。
 *
 * ── 為什麼抽出來 ──────────────────────────────────
 *
 * 第 6 輪（第三十一圈）量到：`check-copy.mjs` 裡的排除清單是拿去過濾
 * `RULES` 的，而它要排除的 `unused-i18n-key` **從來就不在 `RULES` 裡**
 * （它在 `EXTRA_RULE_IDS`）—— 那個過濾器一條都沒濾掉。
 * 結果是對的，但理由跟程式做的事不是同一件事。
 *
 * 改成作用在全部 8 條上之後，**新舊兩種寫法在今天的資料上結果一樣**
 * （`RULES` 的 5 條剛好就是「全部扣掉排除的」那 5 條），
 * 所以突變掃描分不出來 —— 除非能餵一組「多了一條沒被排除的 extra」的資料。
 *
 * 純函式就能餵。這也正是這個檔案存在的理由。
 *
 * @param {string[]} allIds 全部的規則 id（語料的 ＋ 其他的）
 * @param {Map<string, string>} excludedWhy id → 為什麼它不是寫作約定
 * @returns {{ required: string[], excluded: string[], unknown: string[] }}
 *   `unknown` 是**排除清單裡有、但根本不是規則**的 id ——
 *   那種項目會讓「5 ＋ 3 ＝ 8」看起來成立而實際上在數不存在的東西。
 */
export function documentationDuty(allIds, excludedWhy) {
  const ids = new Set(allIds);
  return {
    required: allIds.filter((id) => !excludedWhy.has(id)),
    excluded: allIds.filter((id) => excludedWhy.has(id)),
    unknown: [...excludedWhy.keys()].filter((id) => !ids.has(id)),
  };
}
