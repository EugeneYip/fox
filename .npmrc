# 專案層的 npm 設定 —— 這裡只放**在任何機器上都成立**的東西。
#
# ⚠ 不要在這裡設 `cache=`。2026-09-04 第一次推上 GitHub 時，這個檔案裡有
# `cache=/Volumes/Projects/.npm-cache`（站主那台 Mac 的外接碟路徑），
# runner 上的 npm ci 當場失敗：
#
#     npm error EACCES: permission denied, mkdir '/Volumes'
#
# 機器專屬的設定放 ~/.npmrc。
fund=false
audit=false
