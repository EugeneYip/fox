# ─────────────────────────────────────────────────────────────
# 全部都是「選填」。沒有填的來源會被自動略過，網站照樣能 build。
# 本機使用：複製成 .env（.env 已在 .gitignore，不會被上傳）
# GitHub 使用：Repo → Settings → Secrets and variables → Actions
# ─────────────────────────────────────────────────────────────

# YouTube Data API v3 金鑰 —— 選填的「備援」，不是必要的。
#
# 平常同步走官方 Atom feed，免金鑰、免配額，會給最近 15 支影片。
# 這個金鑰只在兩種情況用得上：
#   1. 那個 feed 端點掛掉時（實測過會間歇性 404）自動退回 API
#   2. 想往回抓 15 支以外的完整歷史
#
# 申請：https://console.cloud.google.com/ → 啟用 YouTube Data API v3 → 建立 API 金鑰
YOUTUBE_API_KEY=

# 自架或公用 RSSHub 位址，用來橋接沒有官方 RSS 的平台（Threads、小紅書…）
# 留空則這類來源自動略過。公用實例不穩定，長期建議自架。
RSSHUB_BASE=
