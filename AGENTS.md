# AGENTS.md

**給任何要在這個 repo 上工作的人或 AI** —— Claude、ChatGPT／Codex、Cursor、
Grok、Copilot，或人類。不管你是哪一個，規矩是同一套。

這一份是**入口**，不是規矩的正本。正本在 [CLAUDE.md](CLAUDE.md)。

---

## 三十秒版本

`bellafoxy.com` —— 中文古典詩詞的個人網站，站名「狐說八道」。
Astro 7 純靜態站，部署到 GitHub Pages（repo `EugeneYip/fox`，**公開**）。
站主是 Eugene，網站是做給女朋友的：她讀中文系、做詩詞短影音、喜歡狐狸。

動手之前讀完 [CLAUDE.md](CLAUDE.md)；動手之前先知道**現在到哪了**，
那在 [docs/STATE.md](docs/STATE.md)。

改完，這兩個都要全綠才算過：

```bash
npm run verify:all && npm run test:tools
```

做完一個段落就 `git commit`（本機）。**不要 `git push`** —— 站主自己推。

---

## 讀什麼，照什麼順序

| # | 檔案 | 它回答什麼 |
|---|---|---|
| 1 | 這一份 | 你是誰、要照什麼順序讀、不可違反的是哪幾條 |
| 2 | [CLAUDE.md](CLAUDE.md) | **在這裡寫程式的規矩** —— 改東西該去哪個檔案、已知的坑、語氣約定 |
| 3 | [docs/STATE.md](docs/STATE.md) | **現在到哪了、接下來做什麼** |
| 4 | [docs/REVIEW-LOG.md](docs/REVIEW-LOG.md) | 週期性自我檢查的規則與全部歷史。**最後一行寫著下一輪要做什麼** |

其餘：[ARCHITECTURE.md](docs/ARCHITECTURE.md)（設計理由與刻意沒做的事）、
[PRIVACY.md](docs/PRIVACY.md)、[DEPLOY.md](docs/DEPLOY.md)、
[CONTENT.md](docs/CONTENT.md)（怎麼寫一篇）。

---

## 不可違反的四條

違反就是 bug，不是風格問題。

1. **repo 是公開的，個資不能進 repo。** 真實的本名、學歷、email 只存在
   `src/config/identity.local.ts`（在 `.gitignore`）。取值一律透過
   `reveal()`，不要在頁面裡硬寫。`npm run audit:privacy` 與 CI 都會擋。
2. **零第三方請求。** 不加字型服務、分析工具、CDN、直接的 YouTube iframe。
   影片一律走 `VideoFacade`（按了才載入）。
3. **只有中文與英文，不要加日文。** 站主 2026-09-02 明確說過。
4. **不要編造她的平臺帳號。** 網站上只有 YouTube（@FoxPoetry）一個來源，
   `src/config/sources.mjs` 只有一筆，那是事實不是待辦。
   另外有一個平臺的帳號**是知道的**，但站主 2026-09-04 說不放上網站，
   **帳號名也不寫進這個公開 repo**。細節見 `src/config/sources.mjs` 開頭。

---

## 這個專案是怎麼運作的

### 狀態全在檔案裡

隨時可能換人（或換 AI）接手，所以**任何做完的事都要留下痕跡**：
改了東西就更新對應的文件，跑完一輪檢查就寫進 `docs/REVIEW-LOG.md`。

判準很簡單：**如果現在斷線，接手的人光看檔案能不能完全接得下去？**

半成品也要 commit，但 commit message 要寫清楚哪裡沒做完。
留下一個註明「未完成」的檢查點，比留下一堆沒有紀錄的改動好。

### 週期性自我檢查（`/loop`）

這個專案用一個輪替式的自我檢查在維護：**一次只深入一個面向**
（無障礙、效能、內容結構、feed 實測、隱私、文案、建置、視覺 —— 八項），
而不是每次都全掃一遍。

接續的方法，不管你是什麼工具都一樣：

1. 讀 `docs/REVIEW-LOG.md`，找到「紀錄」區**最後一行**的
   「下一輪：N — 面向」。
2. 做那一輪。規則在同一份文件的「每一輪的規則」那一節。
3. 把該輪的紀錄依既有格式 append 到檔案末尾，結尾寫下一輪的編號與面向
   （走完第 8 輪回到第 1 輪）。
4. 更新 `docs/STATE.md`：**只改那兩處** —— 最上面的「最後更新」，
   與「下一輪」那一行。
   逐輪的內容寫進 `REVIEW-LOG.md` 就好，**不要再抄一份到 `STATE.md`**。
   （2026-09-04 之前那裡累積了 200 筆輪次摘要，佔了那份文件的 92% ——
   一份「現在到哪了」長到 2,715 行，就不再是那份文件了。走完一整圈的時候，
   才在「每一圈問過什麼」那張表加一列，並換掉「最近一圈」那一段。）
5. 跑那兩個驗證指令，全綠之後 `git commit`（本機）。

歷史**不重寫**。每一輪的紀錄都留著，包括做錯的那些 —— 那是這份文件最有用
的部分。

### 這個 repo 的標準手法

- **要實際量測，不要憑感覺。** 說對比度不足就把數字算出來，說慢就把
  bytes 貼出來。
- **突變掃描**：加了一條檢查之後，把它守的東西故意弄壞，確認它真的會紅。
  綠燈要能分辨「因為對」與「因為空」。
- **讓工具自己說出來**：人眼看出來的問題，盡量變成一條會擋的檢查。
- **訊息要能照著做**：檢查印出來的話要帶「改法：」，而且要指向改得動的地方
  （`dist/` 底下是建置產物，指那裡等於沒指）。

### 兩個反覆出現的失敗模式

先知道這兩個，可以省下很多時間：

1. **用正則去解析結構化資料。** 這個 repo 記錄過九次以上，每一次都是同一種
   痛：HTML 的巢狀、屬性名裡的連字號（`data-src` 被讀成 `src`）、
   YAML 的區塊純量。要嘛用真的剖析，要嘛讓抽取方式自己驗自己。
2. **探針錯了跟工具錯了長得一模一樣。** 下結論之前先確認你的量測方式是對的
   —— 光是第十八圈就發生過七次。

---

## 如果你不是 Claude Code

Claude Code 會自動載入 `CLAUDE.md`；**其他工具不會**。所以：

- **先自己把 [CLAUDE.md](CLAUDE.md) 整份讀完**，那裡有改東西該去哪個檔案的
  對照表，以及七個已知的坑（其中幾個會讓你花半天找錯方向）。
- 如果你的工具有自己的規則檔機制（Cursor 的 rules、Codex 的設定檔⋯⋯），
  讓它指向 `CLAUDE.md`，不要再抄一份 —— 抄一份就是「同一件事兩個地方」，
  而這個 repo 已經因為那件事付過好幾次代價。
- **暫存檔不要寫進 repo。** 要放中間結果的話放在 repo 之外。
- **不要 `git push`。** 站主的帳號才推得上去。

---

## 給人類接手者

上面的都適用。另外：

```bash
npm install
npm run dev          # 開發
npm run verify:all   # 六道關卡
npm run test:tools   # 工具自己的測試
npm run ci:sim       # 照 deploy.yml 的順序在本機跑一次
```

`npm run ci:sim` 最後會比對「你本機建的 `dist/`」與「從版控建的 `dist/`」。
不一樣通常是因為你有 `src/config/identity.local.ts` —— 那個檔案在
`.gitignore` 裡，**它的值只會出現在你自己的機器上，線上不會有**。
