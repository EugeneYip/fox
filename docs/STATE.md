# 現在到哪了

> **接手這個專案的人（不管是人還是 AI）先讀這一份。**
> （完整的接手指引在 [AGENTS.md](../AGENTS.md) —— 非 Claude 的工具不會自動
> 載入任何規則檔，那一份會告訴你要自己讀哪幾份。）
> 這裡只寫「現在的狀態」與「接下來做什麼」。設計理由在 [ARCHITECTURE.md](ARCHITECTURE.md)，
> 上線步驟在 [DEPLOY.md](DEPLOY.md)，寫作方式在 [CONTENT.md](CONTENT.md)。
>
> 最後更新：2026-09-11（**網站已上線：https://bellafoxy.com**，DNS 與 HTTPS 都好了；
> **第五十四圈進行中：最壞的輸入下長什麼樣？**　站主當天另外指定了一次
> 視覺改版，見下面「站主指定的事」）

---

## 一分鐘理解

`bellafoxy.com` —— 一個中文古典詩詞的個人網站，站名「狐說八道」。
Astro 7 純靜態站，要部署到 GitHub Pages（repo `EugeneYip/fox`，**公開**）。

站主是 Eugene，網站是做給女朋友的。她讀中文系、做詩詞短影音、喜歡狐狸。

## 先確認專案是健康的

```bash
npm install    # 第一次
npm run verify:all
```

這一個指令會依序跑：型別檢查 → 隱私稽核 → 對比度 → 建置 → 無障礙 → 效能預算。
**六道全過才算健康。** 任何一道紅燈就先修那個，不要往下做新東西。

工具本身還有一組測試，不算在六道裡但 CI 會跑（部署前也會）：

```bash
npm run test:tools    # 全部 44 步，零網路請求
npm run test:units    # 不需要 dist 的那 40 步
npm run test:built    # 需要 dist 的那 4 步（要先 build）
```

想知道「內容變多會不會壞」的話：

```bash
npm run fixtures -- 500 --videos 400  # 灌 500 篇假內容 + 400 支假影片
npm run build && npm run check:perf # 量
npm run fixtures -- --clean         # 一定要清乾淨
```

`--clean` 會自己確認零殘留，有殘留會直接失敗。

跑不起來的話檢查 Node 版本。`package.json` 的 `engines` 要求 `>=22.19.0`
（那是相依套件 undici 的實際需求），`.nvmrc` 寫的是主版本線 `22`。

---

## 完成了什麼

網站本身是**完整可用**的，不是骨架。

- 44 個頁面，中文與英文兩種語言
- 四種內容類型：詩詞（有原文／注／白話／朗讀）、文章、短札、外站收錄
- 詩詞直排（`writing-mode: vertical-rl`），窄螢幕自動橫排
- 五個列表頁全部分頁（每頁 30 筆）、系列文章導覽、封面圖（自動轉 WebP 並帶尺寸）
- 24 個發表平臺的目錄與聚合層（建置時抓、結果進版控、平臺掛掉不影響網站）
- 站內搜尋、RSS（兩種）、sitemap、robots、深淺色主題
- 零第三方請求、零追蹤（由 Content Security Policy 強制，不只是約定）
- 六道自動檢查關卡，都接進 CI（無障礙 31 條規則、效能 11 條預算、
  對比度 44 組 + 列印的 color-scheme 覆蓋），
  外加工具的單元測試（`npm run test:tools`，44 步 —— 節流、HTTP header、
  剖析、正規化、同步編排、隱私規則、無障礙規則、效能預算、內容管線規則、
  文案慣例、建置管線規則、對比度，加上 4 步需要 `dist/` 的。
  在 CI 與部署路徑上，但不算在六道內）

  （這幾個數字 2026-09-06 重新量過。在那之前寫的是 24 條、42 組、26 項 ——
  **沒有東西在守它們**，而每一支關卡自己都印得出現在的數字。）
- **每一條規則與預算都有「會響」的測試案例。** 加規則沒加案例，測試會直接失敗
- 顏色用 `light-dark()` 定義，深淺兩套值寫在同一行 —— 不可能漂移

有 5 篇種子內容（3 首詩、1 篇文章、1 則短札），是真的寫過的，
可以直接留著，也可以當範本改。

## 沒完成什麼

| | 狀態 | 卡在哪 |
|---|---|---|
| **推上 GitHub** | ✅ 已推 | 2026-09-04 推上 `EugeneYip/fox`（先 private 檢查、再轉公開）。213 個 commit 壓成 1 個（`a67a0a0`），壓縮前後 tree hash 一致，私密檔案從未進過歷史。Pages 已啟用（`build_type=workflow`）、`cname=bellafoxy.com`，部署 workflow 綠燈。**「接不到 GitHub」那句是錯的，已實測推翻** |
| **DNS 切到 GitHub Pages** | ✅ 已完成 | 2026-09-04 做完。刪掉 apex 與 `www` 兩筆指向舊站的 URL 轉址（Porkbun 會連帶清掉它自己建的 ALIAS／CNAME，24 → 20 筆），新增 4 筆 A（`185.199.108–111.153`）＋ `www` CNAME → `eugeneyip.github.io`。**MX 2 筆與 SPF TXT 完好、五個 vanity 轉址（`ig`／`instagram`／`poetry`／`youtube`／`yt`）完好。** 實測：apex 回 200、`www` 301 導到 apex |
| **HTTPS** | ✅ 已上線 | 2026-09-04 18:48 UTC Let's Encrypt 簽出憑證（`CN=bellafoxy.com`，效期到 12月3日），`https_enforced=true` 已開。四條路徑都收斂到 `https://bellafoxy.com/`：apex HTTPS 200、`www` HTTPS 301、apex HTTP 301、`www` HTTP 301。**DNS 改完到憑證簽出約 15 分鐘** |
| **YouTube 影片同步** | ✅ 已在跑 | 走官方 Atom feed，**不需要金鑰**。已同步 9 支影片。⚠ 那個 feed 只回**最新 15 支**（第 4 輪〔第十一圈〕用大頻道實測），所以**第 16 支之後**更早的影片這條路就看不到了 —— 到時候才需要 `YOUTUBE_API_KEY` |
| **頻道本身** | ⏸ 停更中 | 最後一支影片 2024-10-26。**站主知道，是刻意的**（2026-09-04 確認）。同步到的 9 支全是短影音詩詞（王昌齡、杜甫、李白⋯），確認是他們自己的頻道。另外有一個長影片頻道是別人的，只是其中一支提到她 —— **那個不在來源清單裡，也不該加** |
| **她其他平臺的帳號** | ⏸ 刻意不放 | 網站上只有 YouTube 一個來源。2026-09-04 整理 DNS 轉址時看到**另一個平臺的帳號**，站主當天明確說不可以放上網站 —— 所以那個帳號名**不寫進這個公開 repo**（寫進註解等於發佈它）。理由從「不知道」變成「知道，而且刻意不放」，結果一樣是一筆 |
| **英文文案** | ✅ 已校 | 第 6 輪逐句校過。但英文版目前沒有**內容**，只有介面 |

---

---

## git 歷史：已經壓成一個乾淨的 commit（2026-09-04）

**做過了。** 原本的問題是：早期的 `scripts/audit-privacy.mjs` 把四條身分規則的
真值寫死在正則裡（`pattern: /她的本名/g`），而那個檔案自己在 ALLOWLIST 裡 ——
唯一含有個資的檔案剛好豁免於自己的檢查。第 5 輪（第二圈）修好了**程式**
（值改從 gitignore 的 `identity.local.ts` 或 `PRIVACY_NEEDLES` 取得），
**但已經進到 git 歷史的東西，改程式救不回來。**

2026-09-04 把 213 個 commit 壓成一個。選這個做法的理由：

- **不需要知道那些值就一定清得乾淨** —— 針對性重寫（`git filter-repo`）要先
  列舉出每一個字串，而這台機器上沒有 `identity.local.ts`，`check:history`
  跑不了（它會誠實地說「什麼都沒有檢查」並 exit 1）
- **連 commit message 一起清掉** —— 第三圈第 5 輪記過「commit message 裡的
  個資，檢查工具看不到」
- **輪次紀錄不靠 commit 歷史保存**，它在 `docs/REVIEW-LOG.md` 這個檔案裡

代價：失去 213 筆的 `git blame`／`bisect`。

### 還沒做完的最後一步

壓完之後，舊的 commit 物件**還在 `.git` 裡**（reflog 指得到），只是不再被
任何分支指到。正常的 `git push` 只會送出「指得到的」物件，所以推上去不會帶著它們。
但要真的從這台機器上刪掉，還要跑：

```bash
git reflog expire --expire=now --all && git gc --prune=now
```

**跑完就救不回來了**，所以留給你自己決定什麼時候跑。
（在那之前，如果發現壓錯了，`git reflog` 找得回舊的 HEAD。）

### 推之前的最後確認

```bash
npm run check:history                  # 需要 identity.local.ts 或 PRIVACY_NEEDLES 才跑得動
git ls-files -- src/config/identity.local.ts   # 要沒有輸出
                                              # （identity.local.example.ts 是刻意公開的範本，不算）
npm run verify:all && npm run test:tools
```

`check:history` 查**三個地方**：檔案內容（`git log -S`）、commit message、
以及 tag／branch 的名字。`-S` 看不到只寫在訊息或 ref 名字裡的字串，
第 5 輪（第三、四圈）實測確認過。沒有值可搜尋時它會 exit 1 並說
「什麼都沒有檢查」，不會給假的綠燈。

（那支腳本刻意不叫你自己下 `git log -S"名字"` —— 那會把本名留在 shell 的
歷史紀錄裡，為了查個資外洩而製造另一份個資。）

## 上線這條路走到哪了

完整步驟與疑難排解在 [DEPLOY.md](DEPLOY.md)。2026-09-04 的進度：

0. ✅ **清掉 git 歷史裡的個資** —— 213 個 commit 壓成 1 個。
   只剩一個選擇性的收尾（在 Eugene 那台機器上跑）：
   `git reflog expire --expire=now --all && git gc --prune=now`
1. ✅ **推上 GitHub** —— `EugeneYip/fox`，先 private 檢查、確認乾淨後轉公開。
   `PRIVACY_NEEDLES` secret 已設（Eugene 自己填的值），CI 上實測有生效：
   輸出寫「身分規則：8 個值，來自 PRIVACY_NEEDLES」
2. ✅ **開啟 GitHub Pages** —— `build_type=workflow`、`cname=bellafoxy.com`，
   部署 workflow 綠燈，站台實測回 200（36 KB，標題「狐說八道」）
3. ✅ **改 Porkbun 的 DNS** —— 見上面那張表。做法上有一個坑值得記：
   **不要直接刪 DNS 記錄，要刪「URL 轉址」設定本身**
   （`/api/domains/deleteDomainForwarding`）—— Porkbun 會連帶清掉它為那筆
   轉址建的 ALIAS 與萬用字元 CNAME。直接刪記錄會留下孤兒設定
4. ⏸ **勾 Enforce HTTPS** —— 要等 GitHub 簽出憑證才能開。
   現在 `https_enforced=false`，提早開會拿到「憑證還不存在」

每次改動之前都要跑 `npm run audit:privacy`，確認沒有個資外洩。

---

## 版控狀態

本機有 git 紀錄，remote 是 `EugeneYip/fox`，**2026-09-04 已經推上去了**。

```bash
git log --oneline    # 看目前的檢查點
git status           # 應該是乾淨的
```

**2026-09-06 起，每完成一輪檢查就 commit 並推上去**（站主當天授權，
原本的規則是「只做本機 commit」）。所以隨時中斷都不會掉進度，
而且遠端跟本機不會再差上百個 commit —— 授權那天一次推了 164 個。

**推之前一定要先 rebase。** 排程的 `sync-feeds` 一天跑兩次，
而**最多 commit 一次**（內容沒變、而且今天已經同步過了就不寫檔）。
第 4 輪（第四十七圈）實測：4 次排程、3 筆 commit。本機仍然可能落後一筆：

```bash
git pull --rebase origin main && git push
```

撞到的一定是 `syndication.json`（兩邊只差時間戳）。它是
`scripts/sync-feeds.mjs` 產生的，取遠端那一份：

```bash
git checkout --theirs src/data/syndication.json && git add src/data/syndication.json && git rebase --continue
```

推完立刻確認個資檔沒被帶上去（跟上面「推之前的最後確認」同一條，
**不要**寫成 `git ls-files | grep -c identity.local` —— 那一條永遠是 1，
`identity.local.example.ts` 是刻意公開的範本，它自己就會配到）：

```bash
git ls-files -- src/config/identity.local.ts   # 要沒有輸出
```

---

## 站主指定的事（2026-09-11）

**一、整站的設計語彙。** 「更現代化、更有設計感、更符合主題，
有中國文化元素也有狐狸元素。」做法與每一個決定的理由寫在
[ARCHITECTURE.md](ARCHITECTURE.md) 的「借自線裝書的四個東西」——
魚尾（分節標記）、版框（詩的原文）、版心（頁尾）、籤條（標籤），
外加首頁題辭改直排。動它們之前先讀那一段。

**二、「中文」就是正體中文。** 語言標籤從「繁體中文」改成「中文」
（`site.ts` 的 `LOCALE_LABEL`）。`<html lang>` 與 hreflang 仍然是
`zh-Hant-TW` —— 機器需要那個精確度，讀者不需要。

**三、站上的中文要是臺灣自然會寫的中文。** 這是一條**長期的要求**，
不是一次性的修正，所以做成了檢查：`check:copy` 的 `mainland-wording`。
加的時候實測過，63 頁的可見文字加上所有文件**一個命中都沒有** ——
它不是為了修什麼加的，是為了讓現在這個狀態留得住。
清單刻意很短，臺灣有正當用法的字都不在裡面（理由逐條寫在
`scripts/lib/copy-rules.mjs` 那條規則上面）。

**四、最下方不要用「狐」代表。** 頁尾那一方朱印拿掉了，換成版心的魚尾。
詩與文章末尾那一方**留著** —— 那是落款不是標誌，站主要拿掉的話一行就好
（`EntryLayout.astro` 與 `poems/[...slug].astro` 各一處 `<FoxSeal>`）。

**五（續）、整體視覺再升級。** 站主 2026-09-11 說「期待的並不只是各處的
小修改，而是非模板網站感覺的整體視覺呈現的大升級」，並點名捲動的畫面、
畫面細節、游標。做了五件，全部是 CSS（內嵌 JS 只剩 89 B）：
宣紙的纖維、界欄、跨頁轉場、捲動時的動作、游標。
每一件的做法與代價寫在 [ARCHITECTURE.md](ARCHITECTURE.md) 的
「材質、動作、跨頁」—— 尤其那條
**`animation-timeline` 不可以跟 `animation` 簡寫寫在一起**，
踩過一次而且六道關卡都沒響。

**五、斷句要合邏輯。** 中文改成 `word-break: keep-all`，只在標點處斷；
長文（`.prose`）退回預設。四種做法的實測、兩個代價的數字、
以及為什麼 `.prose` 是例外，都寫在
[ARCHITECTURE.md](ARCHITECTURE.md) 的「斷句：只在標點處斷」。

**五之二、標點落單（2026-09-11 收掉）。** 站主看到
「⋯還有一點自己的話／。」—— 一個句號自己站一行。這件事**猜了兩輪都沒猜中**
（先怪 `overflow-wrap: anywhere`，換成 `break-word`；量出來才知道那兩個值
在 WebKit 上一模一樣，那一改對症狀毫無作用）。第三次他說了「我是用 safari」，
於是拿真的 WebKit（`WKWebView`）把 19 頁 × 12 種寬度量了一遍：

- `word-break: keep-all` 跟 `overflow-wrap` 的保險**在 WebKit 上不能同時在**，
  同時在就會把 `。`「，」推到行首。Chrome 沒有這個行為。
- 改成「寬螢幕 keep-all、不放保險；窄螢幕（`max-width: 34em`）退回預設斷法、
  保險放那裡」，另加五個長文例外（`.synd__*`、`.poem__annotation dd`、
  `.poem__plain`、`.article__lead`），並把兩個被內容撐開的格線軌道
  改成 `minmax(0, 1fr)`。
- 結果：228 格裡**行首禁則違規 0、整頁橫向捲動 0、文字超出視窗 0**
  （改之前分別是 41／153、13／153、有）。
- `check:content` 的 **`punct-orphan-risk`** 在守那個組合不會被裝回去，
  突變掃描驗過會紅。

**五之四、整體視覺升級（2026-09-11 做完）。** 研究、逐項做法與代價都在
[VISUAL.md](VISUAL.md)，第六節九項全部做完：字級層級、詩的形狀、
旁注＋版框放大、影片框、著重號、書口、跨頁轉場、空處給入口、引詩。
整輪 CSS 從 10.9 長到 11.5 KB（上限 14），**CSP 雜湊反而 36 → 35**
（移掉的 `/colophon` 本來是最多的那一頁），內嵌 JS 一行沒加。

同一天站主還砍掉了三塊對一般讀者多餘的東西：`/colophon` 整頁、
頁尾的 RSS 連結（feed 本身留著）、隱私頁的兩節。
判準是**「讀詩的人會不會想知道」**—— 對內的、技術的、後設的資訊不進頁面。

跨頁轉場的動畫是**站主在 Safari 上看過才確認的**：那支 WebKit 探針
拍不到跨文件轉場的中間影格，也沒辦法用 `localStorage` 跨次傳結果。

**五之三、倒十字架（2026-09-11，五之二的後遺症）。** 拿掉保險之後站主第一眼
就看到首頁題辭壞了：「青青子衿，悠悠我心」擠成一條直線，出處那一行橫著穿過去。
量到的是 **`keep-all` 在 `vertical-rl` 底下根本不產生斷點**（橫排會在標點斷，
直排不會），九個字一整欄、往下溢出盒子 112px；**而且縮小盒子沒有用**
（`max-inline-size` 越小溢出越大）。改成 `word-break: normal` 就回到原本的兩欄。

上一輪 228 格全綠卻漏掉它，是因為三個判準**全部是橫向的** ——
直排往下溢出，`scrollWidth` 與「超出視窗右緣」都看不見。
補了一支往下量的，全站直排的溢出只有這一處。
`check:content` 的 **`vertical-keep-all`** 要求每個直排區塊自己寫出
`word-break`（不限定值，要的是「有做決定」）。

量法與整段推導在 [ARCHITECTURE.md](ARCHITECTURE.md) 的
「標點落單：`keep-all` 跟 `overflow-wrap` 不能同時在」與
「直排底下 `keep-all` 不產生斷點」。
**探針本身還在 scratchpad，沒有進 repo**（見 [TODO.md](TODO.md)）。
站主還沒有在 Safari 上回看過 —— 這一輪是量到的，不是他確認過的。

**六、圖示的快取。** 站主回報「favicon 仍然是舊版狐狸」。當天實測：
`bellafoxy.com` 供應的四個圖示跟版控裡的**逐位元組相同**，
而版控裡的就是重畫過的那隻 —— **伺服器這一端是對的，舊的那隻活在瀏覽器裡**
（分頁圖示存在瀏覽器自己的 favicon 資料庫，鍵是網址，重新整理換不掉）。
做了三件事：
`<link rel=icon>` 的網址加上檔案內容的短雜湊（`src/lib/icons.ts`）、
`favicon.ico` 從「副檔名叫 .ico 的 PNG」改成真正的 ICO 容器、
以及 `npm run icons -- --check` 接進 `check:generated`
（守的是 2026-09-09 那次「改了狐狸忘了重跑」）。

---

## 週期性檢查（loop）現在跑到哪

**輪替式的 `/loop` 2026-09-11 由站主喊停，停在第五十四圈第 5 輪（隱私）。**
在他說恢復之前，每一次 `/loop` 都只做**視覺與體驗**，不做輪替的八個面向。
恢復的時候從第 5 輪接下去就好 —— 第 1 到第 4 輪的紀錄都在 `REVIEW-LOG.md`。

（第 8 輪〔視覺與版面〕同一天由站主指定**插隊先做**了，
所以那一輪的紀錄在第 3 輪與第 4 輪之間。）

**視覺輪的間隔是 60 秒**，不是工具說明裡那個「閒置心跳 1200 秒」的通則 ——
站主 2026-09-11 明確講過一次。

**第五十二圈走完了**，這一圈問的是「這個東西的名字，跟它實際做的事一樣嗎？」
—— 把名字當成一句承諾（可及名稱、規則 id、規則印的訊息、文件裡的斷言、
CSS token 的層級、對比表上的「這是哪裡」），再去對照它真正的行為。

**八輪裡七輪找到說了謊的名字，而七個裡有五個是規則自己的**：
連結叫「English」卻在 19 頁去英文首頁；「必須是 0」的個資確認指令**永遠是 1**；
「這一頁真正會發出的請求數」數不到 JS 自己發的；「platform 必須對應 id」沒有人在守；
`--patterns` 的 `✓ youtube` 打的是 Google 自家頻道；`csp-unsafe-inline`
11 條指令只看 1 條；「排程最多 commit 一次」四天裡兩天是兩筆；
對比表六列寫「卡片上的⋯」而這個站的卡片不在那個背景上。

**說謊的方向永遠一樣**：把兩個條件收成一個，或讓一個成員代表全部 ——
也就是把「今天剛好成立的那個窄情況」寫成了名字。

**有人守的名字也會一起錯。** 第 8 輪那六個標籤被測試寫進期望值 19 處，
所以改了會紅 —— 而兩份一起錯了很久，因為那個斷言問的是
「輸出跟我記的一樣嗎」，不是「這個名字指得對嗎」。

**第 6 輪那個「沒發現」是最漂亮的一格**：90 組文案裡 12 組從來沒人看過，
而 12 組一組一組都對應到一件已經記著的「這個功能還沒有真的跑過」——
文案沒被看過跟功能沒被跑過是同一件事的兩面。

**新規則 0 條，連續第六圈。**

---

**第五十三圈走完了**，這一圈問的是「動這一處，會牽動哪些地方？那些牽連，有人寫下來嗎？」

**半徑一律比名字大**：一個只用在 2 頁的 `<style>` → 44 頁每一頁 ＋1 個 CSP 雜湊；
一篇的 `publishedAt` → 13／61 個檔（含**另外兩首詩自己的頁面**）；
一筆新影片 → 10／61；一個布林值 → 14／61、60 個外連的 `rel`。
（每一次都先跑對照組：什麼都不改重建一次是 0 個。）

**真正危險的是半徑外面那個「不跟著動」的** —— 改站名時唯一不動的是社群分享圖
（手抄的字串、二進位、連 npm script 都沒有）。改成讀 `site.ts`，產出逐位元組不變。

**有兩處的牽連是「兩邊一起動」，於是比對它們的檢查永遠不會響**：
關掉 `strictReferrerPolicy`，60 個外連掉了 `noreferrer` 而 `/privacy` 的承諾也跟著改；
`aria-current` 同時是無障礙狀態與 CSS 鉤子，四個元件沒有一處寫下來。

**最後一輪換了性質** —— 站主中途要求視覺與 UI/UX 也要有感的質感優化。
第 8 輪動手改了列表整列的回應與頁首（毛玻璃 → 不透明），
而第二件同時收掉一個量了很多圈都沒人動的 ⚠：頁首標語最壞 4.09:1／4.11:1 從此不存在。

**新規則 0 條，連續第七圈。**

**以後每一圈的第 8 輪都是「動手改視覺、而且要看得出來」的輪次**，不是量測輪。

---

**第五十四圈開始了，問的是：這一塊在「最壞的輸入」下長什麼樣？**
判準：最長的字串、最窄的螢幕、最大的使用者字級、空的情況、失敗的情況 ——
挑一個，真的推到那裡，然後看畫面。

**第 1 輪（無障礙）**：320px ＋ 根字級 32px（＝ 200%）時，導覽列
**只看得到三項**（「各處」在 392px、「關於」在 492px），而頁面不會橫向捲 ——
因為那條捲軸被 `scrollbar-width: none` 明確藏起來了，連「還有東西」都不說。
改成換行：預設字級下畫面**一個像素都沒變**（五項本來就排得下），
字級一大就換到三行、五項全部看得到。
中間踩過一個坑：只拿掉 `overflow-x` 會讓整頁溢出 212px ——
窄螢幕的 media block 裡還有一個 `inline-size: max-content`，那是捲動條的機關。
掃過 16 頁都沒有橫向溢出，而那個「0」先驗過判準真的會失敗（塞 900px 進去會溢出 580）。

**第 2 輪（效能）**：把頁面與 feed 填到它們自己訂的上限 —— 站上只有 9 支影片，
**那些上限從來沒有真的被裝滿過**。`/elsewhere` 取 `limit: 20`，當初就是量出來訂的
（註解裡的表寫著 20 筆 → 12.7 KB、**90%**）。今天填滿再量：
高熵文字 13,626 B（**95.0%**）、更壞的取樣 14,080 B（**98.2%，餘裕只剩 256 B**）。
文件那個 12.7 KB 落在我量到的兩端之間，所以**不是量法不同，是頁面本身變重了** ——
而這個漂移看不見：她要發到第 20 支影片才會第一次裝滿。
第一次量 feed 滿載時我還踩了同一個檔案自己記過的坑（複製的假資料壓縮比 13.4:1，
對 gzip 是最好的輸入不是最壞的）；改成長度校準＋重洗之後，
`rss-all.xml` 滿載 28.4 KB —— 文件推估的 27 KB 站得住。

**第 3 輪（內容結構）**：最壞的輸入是**一個很長的標題**（schema 裡 `title` 沒有上限，
而站上最長的標題只有 15 個字，所以「標題長到要換行」從來沒發生過）。
換成 33 個字再量：內頁 `<h1>`、首頁卡片、`<title>`／`og:title` 三個表面都撐得住，
**只有 `/archive` 的類型標記掉了 34px** —— 它是 `align-self: center`，
對齊的是整列不是第一行，於是跟日期斷開、浮在標題中間。
改成 `baseline`（那一列本來就是 `align-items: baseline`），
長標題與真內容都量過，五列的「mark − date」全部是 0。

**第 8 輪（視覺與版面，站主指定插隊）**：同一個問題用在版面上 ——
最長的字、最矮的螢幕、最大的字級，然後看畫面。

站上的**中文段落有兩種行距**，而窄的那一種（1.45）本來是給標題用的：
最刺眼的是文章導言，它跟正下方的內文**同樣是 18.2px**，行框卻是 26.3 對 33.6。
1.45 在中文上不是「比較密」，是兩行之間只隔 5.9px（白話那一段隔 15.4px）。
六處段落改成 `--lh-body`，代價量過（首頁 ＋110px／＋3.1%）。
`.synd__title` 是特例 —— 全站唯一不是為這個站寫的標題（她在 YouTube 上打的字），
390px 上佔 4／5／3／3／4 行；三個值都在瀏覽器裡看過才選 1.65。

**320×256 下黏著的頁首佔掉 42% 的畫面** —— 那正是 WCAG 1.4.10 點名的尺寸
（1280×1024 放大到 400%）。改成 `@media (max-height: 30em)` 不黏；
一般手機（390×844）與桌機（1280×800）一個像素都沒變。

順帶收掉三處「hover 有、鍵盤沒有」的回饋，把 `--lh-loose` 從寫死接回 token
（`check:contrast` 的「未使用」那一段早就在說），
並把圖示的六個顏色從手抄改成讀 `tokens.css`（七個檔案 md5 逐一相同，
而改 `--c-flame` 現在七個全部跟著變 —— 改之前是 0）。

**突變掃描誠實的一格**：把這一輪兩個視覺改動都撤掉，七道關卡全綠 ——
沒有任何一道量得到算繪出來的幾何。記進 TODO，不在這一輪加檢查。

**第 4 輪（平臺 feed 實測）**：最壞的輸入是**她在 YouTube 上打的字** ——
中間沒有經過站上任何一次編輯，而那些字會落在四個規則不同的出口。
只用字元、不用 payload（`&`、`<`、`>`、`"`、`'`、`</script>`，外加一個
**已經是實體的 `&amp;`** 同時測「沒逃到」與「逃兩次」）餵過去：
**四個出口都乾淨**，搜尋結果裡 `querySelectorAll('b,i,script,img,svg')` 回 0 個。

但要做這個量測得自己造一筆資料 —— 那本身是發現：第 8 輪那次去重之後，
**`rss-all.xml` 與搜尋索引的 syndication 那一半今天走不到**（九支影片全部有詩頁）。
結構還在，先被驗過了。

再灌五筆極端條目：空摘要與缺鍵都正確地不算繪；**空標題被 `check:a11y`
當場擋下來**（`empty-heading` ＋ `link-name`，還點名到網址）——
最壞的輸入下那道關卡是活的。未來日期沒有人守，記進 TODO。

突變掃描誠實的一格：把那個手寫的 `escapeHtml()` 改成不逃角括號，
搜尋結果裡真的長出 `<b>`／`<i>`，而**四道關卡全部 exit 0**。

做法：一次只深入一個面向（八個面向輪流），每一圈換一個**問題**去問全站。
**還沒做的事在 [TODO.md](TODO.md)**（一份會被編輯的清單，不是結轉的）。

輪替順序、每一輪的規則、以及全部 429 筆逐輪紀錄都在
[REVIEW-LOG.md](REVIEW-LOG.md) —— 接手某一輪之前先讀那份的最後一筆。

> **這一節刻意只留索引。** 2026-09-04 之前這裡有 200 筆輪次摘要，
> 佔了 `STATE.md` 的 92%，而那 200 筆在 `REVIEW-LOG.md` 都有更完整的版本。
> 一份「現在到哪了」的文件長到 2,715 行，就不再是那份文件了。

### 每一圈問過什麼

| 圈 | 問的問題 |
|---|---|
| 第一圈 | 先把八個面向各走一次，建立基線 |
| 第二圈 | 不是找新問題，是回頭驗第一圈的結論站不站得住 |
| 第三圈 | 檢查本身是不是真的在檢查 |
| 第四圈 | 把第三圈那個問題做完 |
| 第五圈 | 每一個結論，是在什麼條件下量出來的 |
| 第六圈 | 有什麼是從來沒有真的被執行過的 |
| 第七圈 | 先問現有的東西守不守得住，再決定要不要動 |
| 第八圈 | 這個東西證明的，是不是它宣稱的東西 |
| 第九圈 | 這個案例證明的，是不是它宣稱的那一條 |
| 第十圈 | 這個工具結構上看不見什麼 |
| 第十一圈 | 拿真正跑起來的結果當對照組 |
| 第十二圈 | 用不是預設的方式走一次 |
| 第十三圈 | 每一支檢查的案例，是不是都長得太像 |
| 第十四圈 | 同一件事有兩個地方在守，答案一樣嗎 |
| 第十五圈 | 這個綠燈是因為對，還是因為空 |
| 第十六圈 | 這條規則會不會冤枉好人 |
| 第十七圈 | 站主照著這句話做得到嗎 |
| 第十八圈 | 讀者拿到的是什麼 |
| 第十九圈 | 內容變多之後，先壞的是什麼 |
| 第二十圈 | 那些從沒跟真資料跑過的路，走一次會怎樣 |
| 第二十一圈 | 這一條檢查，實際上判斷過多少東西？ |
| 第二十二圈 | 這件事只在我的機器上成立嗎？ |
| 第二十三圈 | 一整件事從頭走到尾，走得通嗎？ |
| 第二十四圈 | 同一件事，我們在幾個地方說過？那幾個地方說的一樣嗎？ |
| 第二十五圈 | 這一格綠燈，有沒有可能是它根本沒有跑？ |
| 第二十六圈 | 如果這件事今天壞了，我們多久之後會知道？是誰告訴我們的？ |
| 第二十七圈 | 這件事，換一個人來做，做得到嗎？ |
| 第二十八圈 | 這件事，是誰決定的？那個人還在嗎？ |
| 第二十九圈 | 第一次做的人跟第一百次做的人，看到的是同一份東西嗎？ |
| 第三十圈 | 這件事，如果現在整個拿掉，會有誰發現？ |
| 第三十一圈 | 這件事，是我們選的，還是它剛好長成這樣？ |
| 第三十二圈 | 這件事第一次做對了嗎？如果不是，第幾次才對？ |
| 第三十三圈 | 這件事是給誰用的？那個人真的會走到這裡嗎？ |
| 第三十四圈 | 這個答案，系統裡已經有了嗎？如果有，為什麼沒用上？ |
| 第三十五圈 | 我怎麼知道這個數字是對的？有第二種算法算過嗎？ |
| 第三十六圈 | 這道檢查的邊界外面是什麼？那裡現在有幾個？ |
| 第三十七圈 | 這一條規則，是誰要求的？寫在哪份文件裡？ |
| 第三十八圈 | 這件事現在靠誰記得？忘了會怎樣？ |
| 第三十九圈 | 這條待辦還活著嗎？ |
| 第四十圈 | 這段東西，站上真的跑過嗎？ |
| 第四十一圈 | 這一課學過了，當時修乾淨了嗎？ |
| 第四十二圈 | 這份清單是誰維護的？漏一個會怎樣？ |
| 第四十三圈 | 這個綠勾的分母是什麼？誰決定了它？ |
| 第四十四圈 | 這件事，站主要自己做嗎？ |
| 第四十五圈 | 這條待辦還在那裡，是因為它還成立嗎？ |
| 第四十六圈 | 這一段如果拿掉，輸出會差在哪裡？ |
| 第四十七圈 | 這一段多久沒有人碰過了？那段時間裡，它的前提變了嗎？ |
| 第四十八圈 | 站上真的看得到的東西，我們量過幾成？ |
| 第四十九圈 | 這道關卡如果安靜地失效了，我們會不會知道？ |
| 第五十圈 | 照著文件做，做得完嗎？ |
| 第五十一圈 | 同一件事，現在有幾個地方各做一次？做得一樣嗎？ |
| 第五十二圈 | 這個東西的名字，跟它實際做的事一樣嗎？ |
| 第五十三圈 | 動這一處，會牽動哪些地方？那些牽連，有人寫下來嗎？ |
| 第五十四圈 | 這一塊在「最壞的輸入」下長什麼樣？ |

**第四十五圈問的是：這條待辦還在那裡，是因為它還成立嗎？**
判準：**挑幾條被抄了很多圈的待辦，回去對照現在的程式 ——
它描述的那件事今天還是真的嗎？**
理由：第四十四圈第 4 輪那條（「`rss` 兩條路沒跑過」）花五分鐘就結掉了，
而它被原封不動抄了十幾圈；第 2 輪那條（「要重量得先推」）在推的權限下放那天
就失效了，而它照樣被抄。**待辦清單是靠複製貼上延續的，沒有人回頭驗過。**
那份清單現在兩百多條、十六條掛著「→ 站主」。

（第四十四圈問的是：這件事，站主要自己做嗎？
八輪問了八條，而**純粹是他的決定只有一條**。那個標籤實際上蓋住三種東西：
真的要他決定、有人做得到只是沒人做、以及誰都做不了。
而被擋住的往往不是決定本身，是**決定所需要的那個事實** ——
第 6、7、8 輪的改法都不是替他決定，是把那個事實補上。）

（第四十三圈問的是：這個綠勾的分母是什麼？誰決定了它？
八輪八個分母，**沒有一個是算錯的** —— 錯的一律是它涵蓋的範圍，
而範圍由一份人挑的東西決定。那些東西只有三種：**副檔名清單**、
**路徑／資料夾清單**、**屬性／樣式清單**，而八輪裡有五輪的兇手是
「清單裡少了一個今天真的存在的名字」。
最貴的一條：**那句用來說明涵蓋範圍的話，自己也有涵蓋範圍** ——
`check:a11y` 有一句專講「我掃不到什麼」，而它只認 `<a href=`。
八輪的修法都一樣：能推導的改成推導，推導不出來的就把「我沒看哪些」印出來。）

（第四十二圈問的是：這份清單是誰維護的？漏一個會怎樣？
八輪八份清單**今天全部是對的**，缺的一律是守它的東西。清單有三種，
風險差很多：**註冊表**漏一項 → 那個東西根本不存在（沒註冊的內容資料夾，
建置成功、頁數沒變、離開碼 0）；**補 0 名單**漏一項 → 規則從計數消失；
**語料定義**漏一項 → 檢查悄悄變窄，**而輸出照樣說 ✓** —— 第三種最安靜。
另外有兩份「清單」不長得像清單：一段註解，與五份翻譯用的對照表。）

（第四十圈問的是：這段東西，站上真的跑過嗎？
「沒跑過」有三種，風險差很多：沒有語料（低，關卡自己會說）、
沒有內容（中，第一次有內容那天會一次全撞上）、
**只在測試裡跑過（高）** —— `check:perf` 那句改法叫人去改一個
早就不存在的 prop，而唯一跑過它的測試斷言的正是那個過期的字。
另一半的價值在「確認沒事」：三條閒置 a11y 規則第一次碰到主體是對的、
整條同意路徑第一次被走過是對的、兩個 CI 假設都已經有人守。）

（第三十九圈問的是：這條待辦還活著嗎？
假設是「清單裡塞了一堆已經解決的東西」，**假設錯了** ——
驗了約四十條，真正不成立的只有兩條半。真正的問題是**數字會漂**
（11→13、5→7、3.4×→3.6×），而且四次撞到「待辦記的只是問題的一角」。）
第三十八圈結束時，「待辦」那份清單有**九十幾條**，而它是**只進不出**的：
每一輪往上加，很少有人回頭確認舊的還算不算數。而那一圈就撞到兩條已經不成立的
（`related` 站上今天 0 筆；「偶發紅燈沒有留下證據」追下去是結構性的、當場修掉了）。
一份沒有人回頭驗的清單會慢慢變成噪音 —— 真的重要的那幾條會被埋掉。
判準：**拿今天的 repo 驗一次 —— 還成立嗎？還是已經被別的改動解決了？**
還成立的留著並補上今天的數字，不成立的**寫明為什麼**再刪掉。

（第三十二圈問的是：第一版做對了嗎？
八輪裡**沒有一次是「判斷寫錯了」** —— 壞掉的全在旁邊：
範圍、分母、統計、以及沒有語料踩得到的那些邊界。）

（第三十一圈問的是：是我們選的，還是它剛好長成這樣？
八輪八個，**沒有一個是「有人選錯了」** —— 最難發現的一種錯是「剛好相等」。）

（第三十圈問的是：如果現在整個拿掉，會有誰發現？
八輪問八次，**七次的答案是「沒有人」** —— 而多數不是因為東西沒用，
是因為沒有人在看。）

（第二十九圈問的是：第一次做的人跟第一百次做的人，看到的是同一份東西嗎？
不是「輸出太長」，是**同一行字對兩種人講的不是同一件事**。）

（第二十八圈問的是：這件事，是誰決定的？那個人還在嗎？
不是「為什麼這樣做」，是**那個決定的有效期限**。）

（第二十七圈問的是：這件事，換一個人來做，做得到嗎？
第一次 clone 下來要跑什麼、缺哪個檔案會發生什麼、錯誤訊息看不看得懂、
`identity.local.ts` 這種「只有站主有」的東西擋住了哪些路、
以及她要發一篇文時，從第一步到看到它上線，說明夠不夠她一個人走完。）

### 最近一圈（第三十八圈：這件事現在靠誰記得）

八輪找到的東西分成三種形狀。

**一、只有描述，沒有人提醒**（第 1、2、8 輪）：記得把探針貼進 console、
記得回來跑 `probe:served`、記得**怎麼寫**那段量測程式。
第 8 輪最細 —— 留下來的是一句描述，而照著它重寫的人要自己決定選取器與走法，
**那正是會出錯的地方**。

**二、警報在它最需要的那一刻走不到**（第 4 輪）：來源全部失敗 → 資料沒變 →
不部署 → `check:content` 不跑 → 「來源已經 N 天沒成功」那個鬧鐘**不會響**。

**三、綠燈與結尾在說謊**（第 5、7 輪）：沒有 `identity.local.ts` 時
身分規則不跑而結尾照樣寫「乾淨」（**而這個 repo 是公開的**）；
`test:tools` 紅的時候輸出結尾是 85 行 `✓`。
後者順帶解釋了第三十七圈的懸案 —— 那兩次我看的是結尾。

**一半的輪次量出來是 0 個問題**（25 個數字沒變、94 個指令沒壞、
覆蓋率 100%、8 種組合都在表上）——
**這一圈的價值不在抓錯，在讓「忘記」變成看得見的。**

完整紀錄在 [REVIEW-LOG.md](REVIEW-LOG.md) 的「第三十八圈走完了」。

---

## 現在還沒解決的

分成兩種：**只有站主能決定的**，以及**迴圈自己會處理的**。

### 只有站主能決定或執行的

| | 事情 |
|---|---|
| 上線 | 推上 GitHub、DNS 切到 Pages（見 [DEPLOY.md](DEPLOY.md)） |
| 收尾 | `git reflog expire --expire=now --all && git gc --prune=now`（壓歷史之後的最後一步，跑完救不回來） |
| 個資檢查 | 本機建 `identity.local.ts` 或設 `PRIVACY_NEEDLES`，否則身分規則**一次都沒有真的跑過**（`identity-value` 永遠是 0） |
| 開關 | `reveal('email')` 有實作、接了開關，但沒有任何頁面畫出來 |
| 版面 | 首頁「在別處」佔頁面高度 42%，要不要少放幾筆 |
| 內容 | 8 個 schema 欄位沒有任何一篇用過（`cover`／`related`／`videoUrl`／`updatedAt`⋯），背後 49 處消費者從來沒跟真資料跑過 |
| 環境 | 本機 Node v22.15.1 低於 `engines` 宣告的 `>=22.19.0` |

### 迴圈自己會處理的（記在各輪的「待辦」裡）

- `--patterns` 與 `ci:sim` 都只有人手動跑才會跑
- a11y 規則沒有反向案例的那幾條（數字問 `npm run test:a11y-rules` 自己印的那一行）
- Tab 順序、螢幕閱讀器朗讀順序、互動目標尺寸：腳本自己說是人工的
- 建置時間沒有任何東西在量（實測 6.3–7.5 秒）
- `check:contrast` 讀不到檔案時丟的是 Node 原始堆疊
- `EXAMPLE-threads.md` 的檔名本身是一個舊誤解的化石
- 斷點 `34rem` 寫了九次（CSS 沒辦法只寫一次）

完整的、帶著當時量測的版本在 [REVIEW-LOG.md](REVIEW-LOG.md) 每一輪的「待辦」。

---

## 接手時最容易踩到的坑

這些在 [CLAUDE.md](../CLAUDE.md) 有完整清單，這裡挑三個最貴的：

1. **repo 是公開的。** 本名、學歷、email 只能放在 `src/config/identity.local.ts`
   （已 gitignore）。`privacy.ts` 裡只有開關，沒有值。取值一律走 `reveal()`。
2. **`astro.config.mjs` 的 `base` 必須是 `/`。** repo 叫 `fox` 但綁了自訂網域，
   網站在網域根目錄。改成 `/fox` 會讓所有樣式和連結壞掉。
3. **`public/CNAME` 不能刪。** 少了它 GitHub 會默默取消自訂網域。
   `deploy.yml` 有一道檢查擋這個。

---

## 有疑問時去哪找

| 想知道 | 看 |
|---|---|
| 為什麼要這樣設計 | [ARCHITECTURE.md](ARCHITECTURE.md) |
| 視覺現在量到什麼、下一步做什麼 | [VISUAL.md](VISUAL.md) |
| 怎麼上線 | [DEPLOY.md](DEPLOY.md) |
| 怎麼寫文章（給不寫程式的人） | [CONTENT.md](CONTENT.md) |
| 隱私開關怎麼調 | [PRIVACY.md](PRIVACY.md) |
| 無障礙那三件人工項目怎麼做 | [A11Y.md](A11Y.md) |
| 支援哪些平臺、handle 要填什麼 | [PLATFORMS.md](PLATFORMS.md) |
| 檢查過什麼、找到什麼 | [REVIEW-LOG.md](REVIEW-LOG.md) |
| 在這個 repo 上寫程式的規矩 | [CLAUDE.md](../CLAUDE.md) |

沒有任何知識只存在對話紀錄裡。上面這幾份就是全部。
